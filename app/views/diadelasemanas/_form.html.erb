<%= form_with(model: diadelasemana, local: true) do |form| %>
  <% if diadelasemana.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(diadelasemana.errors.count, "error") %> prohibited this diadelasemana from being saved:</h2>

      <ul>
      <% diadelasemana.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre, id: :diadelasemana_nombre %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
